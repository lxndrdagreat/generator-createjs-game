language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-createjs-game' ]; then cd .. && eval "mv $currentfolder generator-createjs-game" && cd generator-createjs-game; fi

